package FifthWeek;

//A website needs to track unique visitors. When a user visits the site, their IP address is stored.
// Duplicate IPs should be ignored.

import java.util.HashSet;
import java.util.Scanner;

public class UniqueVisitorTracker {
    public static void main(String[] args) {
        /*HashSet<String> visit = new HashSet<>();
        Scanner scan = new Scanner(System.in);
        String IPs;

        while(true){
            switch (IPs){
                case "add":
                case "update":

            }
        }*/
    }
}
